# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlickrClien' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FlickrClien
  
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'MBProgressHUD'
  pod 'Kingfisher', '~> 3.0’

end
